<footer>
  <p><%= t('copyright') %></p>
  <% if logged_in? %>
  	<%= link_to t('sessions.logout'), logout_path, method: :delete, id: "logout_link" %>
  	<%= link_to t('menu.new_blog'), new_post_path %>
  	<%= link_to t('menu.order_list'), orders_path %>
  <% else %>
  	<%= link_to t('sessions.login'), login_path, id: "login_link" %>
  <% end %>
</footer>